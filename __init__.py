# Backend（観光地検索アプリ）

この backend は、  
**緯度・経度を受け取って、近くの観光地の情報を返す** 役割を担当します。

フロントエンドから呼ばれる API を提供します。

---

## この backend がやること

1. フロントから「緯度・経度」を受け取る
2. Google Maps API を使って近くの観光地を探す
3. （余裕があれば）観光地の説明文を作る
4. 結果をフロントに返す

---

## ディレクトリ構成と役割
backend/
├── app/
│ ├── main.py # サーバを起動するだけ
│ │
│ ├── api/ # フロントとの窓口
│ │ └── spots.py # /spots API
│ │
│ ├── services/ # 実際の処理を書く場所
│ │ ├── google.py # Google Maps API
│ │ └── llm.py # 生成AI（余裕があれば）
│ │
│ ├── models/ # データの形を決める
│ │ └── schema.py # 緯度・経度など
│ │
│ └── core/ # 設定ファイル
│ └── config.py # APIキーなど
│
└── README.md
└── requirements.txt


---

## 各フォルダの説明（かんたん）

### main.py
- backend のスタートボタン
- サーバを起動するだけ
- 処理は書かない

---

### api/
- フロントが呼ぶ URL を書く場所
- リクエストを受け取り、services に渡す
- 結果をそのまま返す

例： `/spots`

---

### services/
- 観光地を探すなど、実際の処理を書く場所
- Google Maps API や生成AIを使う

---

### models/（schema）

ここは **「データの説明書」**を書く場所です。

フロントエンドから送られてくるデータに対して、  
「こういう形で送ってください」という **ルール**を決めます。

#### 何を決めるの？
例えば `/spots` API では次のようなデータを受け取りたいです。

```json
{
  "latitude": 35.6895,
  "longitude": 139.6917
}
このとき schema では、

latitude は「数字」であること

longitude は「数字」であること

どちらも必須であること

をコードで決めます。

schema があると何がいい？

文字列が来たら自動でエラーになる

データの形式がチーム内で統一される

フロントと「この形で送る」と約束できる

👉 バグを早い段階で防げる

ここに書かないもの

Google API の処理

実際のロジック

if 文が多い処理

---

### core/
ここは **「設定置き場」**です。

アプリ全体で共通して使う設定をまとめます。

何を書く？

代表的なのは次のようなものです。

Google Maps API のキー

生成AIの API キー

環境ごとの設定（本番・開発など）

例（イメージ）：
GOOGLE_API_KEY = "xxxxxxxx"
実際には .env ファイルから読み込みます。

なぜ core に分ける？

APIキーを1か所で管理できる

services や api から簡単に使える

「どこに設定があるか」がすぐ分かる

最初は空でもいい理由

まずはダミーで動かすから

APIキーが不要な段階があるから

👉 後から必要になったら追加する場所

ここに書かないもの

API の処理

ビジネスロジック

画面に関係すること
---

### requirements.txt とは？

この backend を動かすために必要な **Python ライブラリの一覧表**です。

FastAPI などのライブラリは、  
Python を入れただけでは使えないため、  
**別途インストールする必要があります。**

requirements.txt は  
「このプロジェクトでは、これらを使っています」  
という **約束リスト**です。

## なぜ requirements.txt が必要？

チーム開発では、  
人によって PC や環境が違います。

もし requirements.txt がないと、

- 誰がどのライブラリを入れたか分からない
- 人によって動いたり動かなかったりする
- 「俺の環境では動く」が発生する

という問題が起きます。

requirements.txt があれば、  
**全員が同じ環境を一瞬で作れます。**

## この backend で使っているもの

現在、以下のライブラリを使用しています。

- fastapi  
  → API を作るためのフレームワーク

- uvicorn  
  → サーバを起動するためのツール

- python-dotenv  
  → APIキーなどを安全に管理するため

- requests  
  → Google Maps API などの外部APIを呼ぶため

## セットアップ方法

backend を動かす前に、以下を実行してください。

```bash
pip install -r requirements.txt
このコマンドを実行すると、
この backend に必要なものがすべてインストールされます。

使用方法
-バックエンド側でサーバーを立てる（これでバックエンド側の準備はOK）
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
